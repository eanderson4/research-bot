"""research_bot — cheap-token deep research: search, fetch (with Kagi
Extract and Wayback escalation), file-pair caching, LLM worker agents,
adversarial verification, usage ledger, and a cross-tier token report.

Submodules import lazily where it matters (see cli.py); import the pieces
you need directly, e.g. `from research_bot import fetch`.
"""

__version__ = "0.1.0"
